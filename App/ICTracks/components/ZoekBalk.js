import React from 'react';
import { View, Text, TextInput } from 'react-native';

export default ZoekBalk = (props) => {
    return (
        <View style={{width: 323, height: 40, borderWidth: 1}}><TextInput editable placeholder="Zoeken..." ></TextInput></View>
    )
}